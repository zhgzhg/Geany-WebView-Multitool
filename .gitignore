# Build output
/build/
/build-linux/
/build-macos/

# macOS Finder litter
.DS_Store
compile_commands.json
*.o
*.obj
*.dll
*.so
*.dylib

# Local download scratch
/.dl/

# Diagnostic crash dumps / debugger output (clip_repro, catch-crash.sh)
/crashdumps/

# Runtime-written HTML-preview scratch (served next to the installed assets)
_htmlpreview.html

# Meson subproject cache & extracted SDKs (wrap files ARE committed)
/subprojects/packagecache/
/subprojects/webview2/
/subprojects/*.wraplock

# Editor/tooling
/.claude/
